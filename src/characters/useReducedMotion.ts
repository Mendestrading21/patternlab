/**
 * Ré-export : la source unique vit dans le design system (accessibilité transverse).
 * Conservé ici pour ne pas casser les imports `@/characters`.
 */
export { useReducedMotion } from '../design-system/useReducedMotion';
